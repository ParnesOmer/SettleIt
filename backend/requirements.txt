fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29
alembic>=1.13
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27

# Added in later milestones (generation + grounded search):
#   google-genai     — Gemini provider
#   groq             — Groq fallback provider
#   tavily-python    — grounded web search
